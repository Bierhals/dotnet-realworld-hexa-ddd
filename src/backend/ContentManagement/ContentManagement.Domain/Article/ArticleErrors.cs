/* using Conduit.Users.Domain.Common;

namespace Conduit.Users.Domain.Article;

static class ArticleErrors
{
    public static Error SlugIsNotUnique()
    {
        return new(
            errorCode: "article.slug.is.not.unique",
            message: "Slug must be unique"
        );
    }
}
 */
